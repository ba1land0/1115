try:
    # Запит числа
    user_input = input("Введіть число: ")
    number = int(user_input)  # Спроба конвертувати в ціле число
    print(f"Ви ввели число: {number}.")
except ValueError:
    # Випадок некоректного введення
    print("Помилка: введене значення не є числом.")
